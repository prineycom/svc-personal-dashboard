# ── Vikunja (Tasks) ─────────────────────────────────────────────────────
# Mock values — replace secrets before use. In production these go in the
# root .env / Dokploy as VIKUNJA_* (see root .env.example).

# Database (shared PostgreSQL)
VIKUNJA_DATABASE_TYPE=postgres
VIKUNJA_DATABASE_HOST=postgres
VIKUNJA_DATABASE_USER=postgres
VIKUNJA_DATABASE_PASSWORD=change-me-strong-postgres-password
VIKUNJA_DATABASE_DATABASE=vikunja

# Security — openssl rand -hex 32
VIKUNJA_SERVICE_SECRET=change-me-vikunja-secret-64-hex-chars-0000000000000000000000

# Public URL — MUST match the domain, or the frontend calls the wrong API host.
VIKUNJA_SERVICE_PUBLICURL=http://tasks.example.com
