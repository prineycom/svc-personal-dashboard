# ── Wger (Health/Fitness) ───────────────────────────────────────────────
# Mock values — replace secrets before use. In production these go in the
# root .env / Dokploy as WGER_* (see root .env.example).

# Django secrets — 50 char random strings
# Generate: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=change-me-wger-secret-key-50-random-chars-aaaaaaaa
SIGNING_KEY=change-me-wger-signing-key-50-random-chars-bbbbbbbb

# MUST be "False" so the entrypoint runs collectstatic on startup.
# If unset/True, the wger_static volume stays empty and nginx serves
# 404 for every /static/ asset.
DJANGO_DEBUG=False

# MUST match the public domain, or login returns 403.
CSRF_TRUSTED_ORIGINS=http://fitness.example.com

# Timezone
TIME_ZONE=Europe/Belgrade
TZ=Europe/Belgrade

# Application
WGER_INSTANCE=https://wger.de
ALLOW_REGISTRATION=True
ALLOW_GUEST_USERS=False
ALLOW_UPLOAD_VIDEOS=True
MIN_ACCOUNT_AGE_TO_TRUST=21

# Exercise/ingredient sync — use Celery background jobs
SYNC_EXERCISES_CELERY=True
SYNC_EXERCISE_IMAGES_CELERY=True
SYNC_EXERCISE_VIDEOS_CELERY=True
SYNC_INGREDIENTS_CELERY=True
SYNC_INGREDIENTS_DUMP_URL=https://wger.de/media/ingredients/ingredients.jsonl.gz
DOWNLOAD_INGREDIENTS_FROM=WGER
CACHE_API_EXERCISES_CELERY=True
CACHE_API_EXERCISES_CELERY_FORCE_UPDATE=True
USE_CELERY=True

# Celery (using shared Redis)
CELERY_BROKER=redis://redis:6379/2
CELERY_BACKEND=redis://redis:6379/2
CELERY_FLOWER_PASSWORD=change-me-flower-password

# Database (shared PostgreSQL)
DJANGO_DB_ENGINE=django.db.backends.postgresql
DJANGO_DB_DATABASE=wger
DJANGO_DB_USER=postgres
DJANGO_DB_PASSWORD=change-me-strong-postgres-password
DJANGO_DB_HOST=postgres
DJANGO_DB_PORT=5432
DJANGO_PERFORM_MIGRATIONS=True

# Cache (shared Redis)
DJANGO_CACHE_BACKEND=django_redis.cache.RedisCache
DJANGO_CACHE_LOCATION=redis://redis:6379/1
DJANGO_CACHE_CLIENT_CLASS=django_redis.client.DefaultClient
DJANGO_CACHE_TIMEOUT=12960
